# ASBP Current State

## Branch

Expected active branch:

`product-rebuild-from-pre-pr23`

## Rebuild baseline

`c17b7893c6e58b8bf3b3651a0232214eb2584956`

Baseline commit title:

`docs: close Phase 8 cloud compute boundary (#22)`

## P0

P0 product roadmap lock was committed locally as:

`2b880c9 product: add build roadmap`

Required roadmap file:

`PRODUCT_BUILD_ROADMAP.md`

## Active product direction

Next product step:

`P1 — Product Application Service Layer`

P1 must be executable code with tests.

## Drift decision

The productization/SaaS drift root was around PR #23 / Addendum 10, before M25 execution.

M25.3 exposed the issue.

The project is now rebuilding from pre-PR-23.

## Salvage rule

Do not automatically reapply old post-PR-23 work.

Useful implementation ideas from later work may be reintroduced only intentionally, through product-roadmap steps, with code and tests.

## PR 134

PR 134 was closed/superseded and must not be merged as the next path.

## Local state rule

The assistant cannot see unpushed local commits unless the user provides local git output.

Always confirm local state before work.
